import styles from "./flights.module.scss";

const Flights = () => {
  return (
    <div className={styles.flights}>
      <div className={styles.container}>Flights</div>
    </div>
  );
};

export default Flights;
